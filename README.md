# Default Authentication services App

By default, creates a set of account database tables in the database and provides a smple log-in form.
Advanced features include optional use of an LDAP directory. 